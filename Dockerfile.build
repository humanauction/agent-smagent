FROM ubuntu:22.04

# Minimal runtime dependencies
RUN apt-get update && apt-get install -y \
    ca-certificates \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /usr/local/bin

# Copy the Linux binary produced by CI
COPY smage-linux ./smage
RUN chmod +x smage

ENTRYPOINT ["./smage"]
CMD ["help"]
